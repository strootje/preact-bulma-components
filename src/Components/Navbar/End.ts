import BuildElement from '../../BuildElement';
import { ModifierProps } from '../../Modifiers';

export interface NavbarEndProps extends ModifierProps {
}

export default BuildElement<NavbarEndProps>('navbar-end');
