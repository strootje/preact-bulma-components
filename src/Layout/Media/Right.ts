import BuildElement from '../../BuildElement';
import { ModifierProps } from '../../Modifiers';

export interface MediaRightProps extends ModifierProps {
}

export const MediaRight = BuildElement<MediaRightProps>('media-right');
